import 'package:flutter/material.dart';

class StudentModeSelectScreen extends StatelessWidget {
  const StudentModeSelectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Placeholder(),
    );
  }
}
